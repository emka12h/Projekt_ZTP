services:
  apigateway:
    image: ${DOCKER_REGISTRY-}apigateway
    build:
      context: .
      dockerfile: ApiGateway/Dockerfile

  guestservice:
    image: ${DOCKER_REGISTRY-}guestservice
    build:
      context: .
      dockerfile: GuestService/Dockerfile

